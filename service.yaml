apiVersion: v1
kind: Service
metadata:
  name: studentsurvey-service
spec:
  type: LoadBalancer
  selector:
    app: studentsurvey
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30684
